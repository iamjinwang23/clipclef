# 개발 세션 요약 — 2026-04-07

## 완료된 기능

---

### 1. 상세 페이지 영상 재생 + 트랙 타임스탬프 seek

**변경 파일**
- `src/features/playlist/components/PlaylistPlayer.tsx` (신규)
- `src/app/[locale]/playlist/[id]/page.tsx`

**내용**
- 정방형 썸네일 → 16:9 YouTube IFrame 플레이어로 교체
- 트랙 클릭 시 해당 타임스탬프로 seek + 자동 재생
- 현재 재생 중인 트랙 하이라이트 (▶ 표시)
- 레이아웃 변경: 제목/채널 → 플레이어 → 트랙리스트 → 태그/버튼 → 댓글

---

### 2. 트랙 start_sec 수동 입력 (어드민)

**변경 파일**
- `supabase/migrations/003_add_start_sec.sql` (신규)
- `src/types/index.ts` — Track 타입에 `start_sec` 추가
- `src/app/api/youtube/route.ts` — 파싱 시 `start_sec` 저장
- `src/features/admin/components/PlaylistForm.tsx` — TrackEditor에 "시작" 컬럼 추가
- `src/features/admin/hooks/useAdminPlaylists.ts` — 타입 및 fetchTracks 업데이트
- `src/features/playlist/components/PlaylistPlayer.tsx` — start_sec 우선, 없으면 누적 계산 폴백

**내용**
- 타임스탬프 댓글 기반 영상 등록 시 어드민에서 `0:00`, `1:23`, `1:23:45` 형식으로 수동 입력
- 자동 파싱된 플리는 start_sec 자동 저장

**DB 변경**
```sql
ALTER TABLE tracks ADD COLUMN IF NOT EXISTS start_sec INTEGER;
```

---

### 3. 검색 (제목 · 채널명 · 트랙 · 아티스트)

**변경 파일**
- `src/features/filter/store.ts` — `query` 상태 추가
- `src/features/filter/components/FilterBar.tsx` — 검색창 추가 (필터 위)
- `src/features/playlist/hooks/usePlaylists.ts` — ilike 검색 + 트랙/아티스트 서브쿼리

**내용**
- 검색창: 제목 + 채널명 `ilike` 검색
- 트랙 제목 + 아티스트명으로도 검색 가능 (tracks 테이블 별도 쿼리 후 playlist_id 매핑)
- 기존 태그 필터와 동시 적용
- 검색어에 SQL 특수문자(`%`, `_`) escape 처리
- reset 시 검색어도 초기화

---

### 4. 유저 재생목록 + 프로필

**변경 파일**
- `supabase/migrations/004_user_playlists.sql` (신규)
- `src/types/index.ts` — `UserPlaylist` 타입 추가
- `src/features/user-playlist/hooks/useUserPlaylists.ts` (신규)
- `src/features/user-playlist/components/AddToPlaylistButton.tsx` (신규)
- `src/app/[locale]/me/playlists/page.tsx` (신규)
- `src/app/[locale]/profile/[userId]/page.tsx` (신규)
- `src/app/[locale]/playlist/[id]/page.tsx` — "내 목록에 담기" 버튼 추가
- `src/components/layout/Header.tsx` — "내 재생목록" 메뉴 추가

**내용**
- 플리 상세 → "내 목록에 담기" 버튼 → 팝오버에서 재생목록 선택/해제 + 새로 만들기
- `/me/playlists`: 재생목록 생성·삭제, 공개/비공개 토글, 항목 확인
- `/profile/[userId]`: 공개 프로필 — 썸네일 그리드로 재생목록 표시, URL 공유 가능

**DB 변경**
```sql
CREATE TABLE user_playlists ( ... is_default BOOLEAN DEFAULT FALSE ... );
CREATE TABLE user_playlist_items ( ... );
```

---

### 5. 컬렉션 → 기본 저장함 통합

**변경 파일**
- `supabase/migrations/005_add_is_default.sql` (신규)
- `src/features/interaction/hooks/useCollection.ts` — 재작성
- `src/app/[locale]/me/playlists/page.tsx` — 기본 저장함 상단 고정, 삭제 불가
- `src/app/[locale]/collection/page.tsx` — `/me/playlists` 리다이렉트
- `src/components/layout/Header.tsx` — 중복 컬렉션 메뉴 제거

**내용**
- 컬렉션 버튼 = "저장한 플리" 기본 저장함에 즉시 담기 (없으면 자동 생성)
- 기본 저장함: `기본` 뱃지, 상단 고정, 삭제 불가, 공개/비공개 전환 가능
- 좋아요(공개 지표) / 기본 저장함(빠른 저장) / 내 재생목록(큐레이션) 역할 명확히 분리

---

### 6. 재생목록 UX 개선

**변경 파일**
- `src/app/[locale]/me/playlists/page.tsx`

**내용**
- 아코디언 독립 동작: `string | null` → `Set<string>` — 여러 목록 동시에 열림 유지
- 항목 호버 시 `→` 이동 버튼 + `×` 제거 버튼 노출
- 이동 버튼 클릭 → 다른 목록 선택 드롭다운 → 즉시 이동 및 양쪽 목록 반영

---

### 7. 항목 드래그앤드랍 순서 변경

**패키지 추가**
- `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities`

**내용**
- 각 항목 왼쪽 점 6개 핸들로 드래그
- 놓으면 즉시 UI 반영 + DB `position` 업데이트
- 모바일: 길게 누른 후(200ms) 드래그
- `PointerSensor` + `TouchSensor` 동시 지원

---

## DB 마이그레이션 목록

| 파일 | 내용 |
|------|------|
| `003_add_start_sec.sql` | tracks에 start_sec 컬럼 추가 |
| `004_user_playlists.sql` | user_playlists, user_playlist_items 테이블 생성 |
| `005_add_is_default.sql` | user_playlists에 is_default 컬럼 추가 |

---

## 다음 우선순위 (미완료)

| 순서 | 기능 |
|------|------|
| 4 | 유튜브 계정에 재생목록 저장 (Google OAuth youtube scope) |
| 5 | 에디터 노트 + 테마 컬렉션 (큐레이션 차별화) |
